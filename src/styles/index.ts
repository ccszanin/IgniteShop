import { createStitches }  from '@stitches/react'


export const {
config,
styled,
css,
globalCss,
theme,
keyframes,
getCssText,
createTheme

}= createStitches({
    theme:{
      colors: {
        rocketseat: '#8257e6',
      }
    }
})